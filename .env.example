# 登录认证配置
AUTH_USERNAME=admin
AUTH_PASSWORD=your_secure_password_here

# 存储配置（三选一，优先级：KV > Redis > 内存）
# 1. Vercel KV（推荐，Vercel 部署时自动配置）
KV_REST_API_URL=
KV_REST_API_TOKEN=

# 2. Redis（自托管或其他平台部署时使用）
REDIS_URL=redis://localhost:6379

# 3. 内存存储（无需配置，重启后数据丢失）
